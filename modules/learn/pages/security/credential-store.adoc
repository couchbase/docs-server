= Credential Store
:description: The credential store lets you securely store and use credentials for authenticating with external systems.
:page-toclevels: 2
:page-edition: Enterprise Edition

[abstract]
{description}
It is for external credentials such as those used to authenticate with AWS S3 or REST APIs.
You only use it to store credentials to authenticate outgoing requests to other hosts. 
It does not play any role in users or clients authenticating with Couchbase Server.
However, you can use it to store credentials users and services use to authenticate with other Couchbase Server or Capella clusters.

== Credential Store Benefits

The credential store makes managing credentials easier while also reducing the risk of exposing them:

* You grant users and groups the ability to authenticate with external systems without disclosing the credential to them. 
The user chooses a credential from the credential store when authenticating with a supported external host. 
If you have granted them permission to use the credential, Couchbase Server's services use the credential to authenticate on the user's behalf.

* You use Role-Based Access Control (RBAC) to grant and revoke users' access to the credentials.
It works the same way you grant them access to other Couchbase features such as buckets.

* You can also directly grant a service access to credentials. 

* The credential store removes the need for hard-coding credentials in scripts and queries. 
These hard-coded credentials are a security threat.
They're also hard to maintain.

* Centralizing credentials simplifies rotating them.
You do not need to notify users of credential updates.
When you update a credential in the credential store, users use the updated credential automatically.
+
Centralization also helps you automate credential rotations.
You can create a script that periodically renews credentials such as access tokens and stores them in the credential store. 
Your applications transparently use the updated credentials without having to managing renewals on their own.

* You can limit how a user can use a credential by setting guardrails on it.
For more information, see <<#guardrails>>.

== Credential Store Uses

The following Couchbase Server features support authenticating with third parties using the credential store:

* xref:learn:services-and-indexes/services/continuous-backup.adoc[Continuous backup] uses the credential store to authenticate with cloud storage locations.
* The {sqlpp} xref:n1ql:n1ql-language-reference/curl.adoc[`CURL()`] function supports using a credential when authenticating with web sites.
* Querying data from Apache Iceberg tables requires credentials to read from the catalog provider and the data store. 
 
// FIXME: add link to external collections doc once written.

== Supported Credential Types

The credential store supports storing specific types of credentials for external systems:


[cols="1,3"]
|===
| Provider | Credential Types

| AWS
| Access keys

| Azure 
| Active Directory, managed identity, SAS, and shared key

| Couchbase
| None, half, and full encryption

| GCP
| HMAC Keys, Service Account Key

| HTTP
| Basic, bearer token, mTLS
|===

Each type of credential has its own required and optional information. 

NOTE: You can create a Couchbase credential type, but currently it cannot be used to authenticate with any Couchbase service. 

== Credential Permissions

You grant users and groups access to credentials using the xref:learn:security/roles.adoc#credential-consumer[Credential Consumer] RBAC role. 
For more information about roles, see xref:learn:security/roles.adoc[].
You add the IDs of the credentials they can use to the role's xref:learn:security/roles.adoc#roles-parameter-list[parameter list].

Granting a user the ability to use a credential does not let them view the credential's secrets. 
Instead, it allows users to ask a service to use the credential on their behalf when they need to authenticate with an external system. 

For more information about letting users use credentials, see xref:manage:manage-security/manage-credentials/manage-user-access-credentials.adoc[].

You can also directly grant Couchbase services access to a credential for their own use. 
For example, you can configure continuous backup to use a cloud-based storage location to store its backup data. 
To let it authenticate with the storage provider, you must grant the Backup Service access to a credential. 

To administer credentials, a user must have the xref:learn:security/roles.adoc#full-admin[Full Admin], xref:learn:security/roles.adoc#credential-admin[Credential Admin], or xref:learn:security/roles.adoc#security-admin[Security Admin] RBAC roles.
In addition, the xref:learn:security/roles.adoc#ro-security-admin[Read-Only Security Admin] can view details about credentials but cannot create, alter, or delete them.

[#guardrails]
== Guardrails

You can limit how a user can use a credential by defining guardrails.  
They let you limit the use of a credential to a specific website or even specific URL (such as a single REST API endpoint).
This limitation prevents exposing the credential to unrelated sites or granting the user access to resources they should not be able to access.

=== Guardrail Restrictions

Couchbase Server supports 2 types of guardrail restrictions on credentials:

[#allowed-services]
Which service can use the credential on behalf of a user::
When creating a credential, you choose which services you allow to use the credential on behalf of a user.
For example, the Query Service needs credentials to authenticate with catalog providers and storage locations when handling user queries of Apache Iceberg external collections.
When creating these credentials, you must add the Query Service to the list of allowed services.
If you do not, the Query Service cannot use the credential on behalf of the user.
The user's attempt to query the external collection returns an error, even if you granted them permission to use the credentials.

+
If you do not add any services to the list of allowed services, then services cannot use the credential on behalf of a user.
In this case, only services you directly grant access can use the credential for their own use.
Guardrails do not apply to an authorized service using the credential for its own use. 

[#url-restrictions]
URL and endpoint restrictions::
A guardrail can allow or deny the use of a credential based on the URI of the remote system.
You can choose to allow services to use the credential to authenticate the user with any URL. 
Or you can supply a list of URLs that the credential is allowed for. 
You can further refine the list of allowed URLs by supplying a list of disallowed URLs to exempt them from the allowed list.

+
For example, suppose you set a credential's allowed URLs list to `\https://example.com/` and its disallowed list to `\https://example.com/forbidden-endpoint`. 
Then a service can use the credential to authenticate on behalf of the user with most URLs on `example.com`, such as `\https://example.com/some-endpoint`.
However, the disallowed list prevents a service from authenticating with the `\https://example.com/forbidden-endpoint` endpoint or any endpoints under it. 

+
If you neither allow a credential to authenticate with all URLs nor supply a list of allowed URLs, users cannot use the credential to authenticate with web services.

=== Guardrail Enforcement

Service guardrails are enforced at 2 levels.

* Couchbase Server enforces a credential's service restriction.
When a user asks a service to use a credential on their behalf, Couchbase Server makes sure that the service is in the credential's allowed services list.
If it's in the service list, Couchbase Server sends the credential to the service.
Otherwise, Couchbase Server returns an error.

* The service performing an action on behalf of the user enforces any URL allow list and block list restrictions.

=== When Guardrails Apply

Guardrails only apply to users using credentials. 
They do not apply when services use credentials that you directly grant to them for their own use.

For example, suppose a user calls the xref:n1ql:n1ql-language-reference/curl.adoc[`CURL()`] function in a {sqlpp} query to make a request to an external system.
They can specify a credential in the credential store for  `CURL()` to use when authenticating with the remote host. 
In this case, the Query Service is using the credential on behalf of the user, so it's subject to guardrail restrictions. 
Before allowing the Query Service to access the credential, Couchbase Server verifies that the credential allows the Query Service to use it on behalf of the user.
If the credential has the Query Service (`n1ql`) in its list of allowed services, Couchbase Server sends the credential to the service.

[#expiration]
== Credential Expiration

You can set an expiration on a credential. 
When the expiration date and time for the credential passes, attempts to use the credential fail with a credential expiration error.

It's good practice to set the credential's expiration to earlier than any expiration built into the underlying authentication credentials.
Doing so provides applications with a more predictable error message in case the credentials expire.

For example, suppose you have a credential for an HTTP bearer token that expires every 24 hours. 
You have a script that automatically renews the token with the authentication provider every 12 hours or so.
The 12 hour renewal margin leaves plenty of room for retrying failed renewals caused by the authentication provider being down or network issues.
After renewal, the script calls Couchbase Server's REST API to update the credential containing the token so that the credential remains valid.

In this case, you can have the renewal script set the expiration for the Couchbase credential to less than 24 hours.
For example, you might choose to have the credential expire in 21 hours.
This value leaves a 3 hour buffer between the expiration of the credential and the bearer token.
With this buffer, any issues with the token renewal result in a Couchbase Server credential error rather than an error from the remote host.
You can then have your applications handle a predictable Couchbase Server error instead of a range of errors that the remote hosts may throw.

== Credential Store Frequent Asked Questions

Does the credential store handle logins or connections from my applications to Couchbase Server?::
No. 
The credential store only holds credentials Couchbase Server services use to authenticate to external systems.
For example, its credentials authenticate with AWS S3, Azure Blob Storage, GCP, or a third-party REST API.
It has no effect on how clients or users connect to Couchbase Server itself.
That continues to be handled by the existing user, RBAC, and certificate-based authentication mechanisms.

I granted a user access to a credential, but they still cannot use it. Why?::
Granting a user the xref:learn:security/roles.adoc#credential-consumer[Credential Consumer] role only authorizes that user to use the credential.
You must also allow a Couchbase Server service to use the credential on the user's behalf.
You configure this using the credential's `allowedServices` guardrail.
If the credentials guardrail's `allowedServices` list is empty, no service can use the credential for any user, regardless of what roles they hold.
Check the credential's guardrails and confirm the service you expect to use it (for example, the Query Service, `n1ql`) is listed.

What's the difference between a `couchbase`-type credential and my Couchbase Server user account?::
They're unrelated. A `couchbase`-type credential stores authentication material (username/password or a client certificate) for connecting to a remote Couchbase cluster.
It does not grant access to the local cluster. 
Your own login to this cluster is still governed by your regular Couchbase Server user account and its assigned roles.

+
NOTE: Currently, no Couchbase service accepts a Couchbase credential.

What happens when a credential expires?::
The credential store does not delete expired credentials. 
Any attempt to consume it fails until you update its expiration. 
You can extend a credential past its original expiration date by updating its `expiresAt` value.
However, when a credential expires, it's usually time to update the credential's secrets. 
The best practice is to set a credential's expiration to reflect the expiration of its secrets.

Can I use the credential store as a general-purpose secrets manager for my own applications?::
No. 
The credential store only supplies credentials to Couchbase Server services and integrations.
It is not a general-purpose vault your own applications can query directly.
Couchbase Server cannot act as a proxy for arbitrary outbound requests.

Why is the secret field always blank when I edit a credential in the Web Console?::
To prevent disclosing secrets, Couchbase Server never displays a credential's sensitive values (passwords, keys, tokens, and so on) once they've been saved.
This matters when you save an edit.
If you leave a secret field empty and save the credential, the stored value is permanently removed. 
If you're only changing the credential's metadata (its description, expiration, or guardrails) you can leave secret material blank.
The Couchbase Server Web Console does not overwrite the credential's secrets if you do not change any credential details such as the account name or access key ID.
You can also use the REST-API's `PATCH` instead of `PUT` to edit the credential's metadata.

Can more than 1 user share the same credential?::
Yes. 
Grant the Credential Consumer role for the same credential ID to as many users or groups as needed. 
Each grant is independent, so you can also revoke a user's access without affecting anyone else.

Do I grant Credential Consumer to a user, a service, or both?::
It depends on how the credential is consumed. 
For credentials a service uses on a user's behalf (such as the Query Service's `CURL()` function or Iceberg external collections), grant Credential Consumer to the user or group.
Also add the service to the credential's `allowedServices` guardrail. 

+
For credentials a service uses as itself, such as continuous backup, grant Credential Consumer directly to that service's identity instead.
You do not need to add the service to the `allowedServices` guardrail because the service is not acting on anyone's behalf.

== See Also

* xref:manage:manage-security/manage-credentials/manage-credential-store.adoc[]
* xref:manage:manage-security/manage-credentials/manage-user-access-credentials.adoc[]
* xref:manage:manage-security/manage-credentials/manage-services-access-to-credentials.adoc[]
* xref:manage:manage-security/manage-credentials/override-credential-store-requirements.adoc[]
* xref:rest-api:security/credential-store/credential-store.adoc[]
* xref:learn:security/roles.adoc[]
