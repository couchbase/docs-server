= Calling a JavaScript UDF
:description: Calling a JavaScript function from N1QL.
:page-pagination: 
:page-edition: Enterprise Edition
:page-topic-type: guide
:page-toclevels: 2

[abstract]
{description}

== Introduction

A JavaScript function can be called like any other N1QL function using a suitable N1QL statement.
The Javascript function is not called directly; it is called through a N1QL reference function.

include::partial$before-you-begin.adoc[]

include::partial$query-tools.adoc[]

== Calling the Reference function

A reference function is called through the Query Workbench, the REST API, or the CBQ shell.

[tabs]
====
Query Workbench::
+
--
. Enter a N1QL statement to call your function in the Query Workbench editor:
+
[source, n1ql]
----
include::example$javascript-udfs/select-get-business-days.n1ql[]
----

. Set the `context` to `unset` so that we can run the query under the global context.
+
image::javascript-udfs/setting-context.png[alt="setting the context to unset"]

Now press the btn:[Execute] button to run the statement.

The statement makes a call to the JavaScript function and returns a result:

.Result
[source, json]
----
include::example$javascript-udfs/execute-get-business-days-response.jsonc[]
----
--

REST API::
+
--
Open a command shell and execute the following command:

[source, console]
----
include::example$javascript-udfs/execute-javascript-function.sh[]
----
include::partial$javascript-udfs/execute-n1ql-result-explanation.adoc[]
--

CBQ Shell:: 
+
--
. Enter the following command to execute the query:
[source, console]
----
include::example$javascript-udfs/execute-javascript-function.n1ql[]
----
include::partial$javascript-udfs/execute-n1ql-result-explanation.adoc[]
--
====


