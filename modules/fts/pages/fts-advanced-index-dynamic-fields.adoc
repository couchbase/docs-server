= Advanced Index Dynamic Fields

When checked, ensures dynamic fields are indexed.

When unchecked, they are not indexed.

If you index the dynamic fileds at the advanced level , all the fields get indexed. 

