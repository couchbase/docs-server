= Manage Passwords
:description: pass:q[Couchbase Server lets you manage passwords for local users, and enforce password policies.]
:page-toclevels: 3

[abstract]
{description}

Couchbase Server manages passwords for users in the local domain when using password authentication.
The administrator, who installs and configures Couchbase Server, can create, reset, and enforce password policies for these accounts through the Web UI, CLI, or REST API.
Local user accounts also support additional security controls such as forced password changes, password expiration, and restrictions on password reuse.

External authentication systems such as LDAP or SAML manage user credentials in the external domain.
Couchbase Server relies on these systems to verify user identity, which means that password creation, storage, and policy enforcement occur outside of Couchbase.
This separation allows organizations to centralize identity management, apply consistent authentication policies across applications, and reduce administrative overhead.

[#user-password-changes]
== User Password Changes

Local users and external users can change their own passwords.

[#password-changes-for-local-users]
=== Password Changes for Local Users

Users defined in the local domain can change their own passwords directly through Couchbase Server from the following interfaces:

* *Couchbase Web Console/Web UI*: If the local domain users have Web UI access, they can sign in to their account and use the *Change Password* option from the account menu.
If an administrator forces a local user to change their password, the user must update it through the Web UI.
Even if the user does not have a role that grants Web UI access, they must still authenticate with the Web UI.
After authentication, the Web UI prompts the user to set a new password.
+
For more information, see xref:manage:manage-security/manage-passwords.adoc#local-user-password-controls[].

* *CLI*: Users can change their password by using the `couchbase-cli user-change-password` command.

* *REST API*: Users can change their password by calling the `/controller/changePassword` endpoint.

This flexibility allows local users to manage their credentials without administrator involvement, which reduces administrative workload and improves security by ensuring that only the user knows their active password.
In addition, Couchbase Server can enforce policies such as password expiration, reuse restrictions, and forced password changes to improve security for local accounts.

=== Password Changes for External Users

External domain users can change their passwords through their identity provider (IdP) or authentication system.

* Couchbase Server does not store or manage external user passwords.
* External users must use external authentication systems such as LDAP or SAML to change their passwords.
* After changing the password in the IdP, external users must use the new password to access Couchbase.

== Administrative Password Controls

Administrators can set initial passwords for users when creating accounts in the local domain.
They can also change passwords for existing users through the Web UI, CLI, or REST API, ensuring that accounts remain secure if credentials are compromised.
In addition, administrators can force a user to change their password at the next login, which is useful when assigning temporary passwords or when stronger security is required.

This feature ensures that users set their own private password as soon as possible.

[#reset-password]
=== Reset Password

You can reset any local user's password associated with your cluster.

NOTE: You cannot reset the password for external domain user accounts.
The [.ui]*Reset Password* button appears on the [.ui]*Users* section only if the user is
locally defined.

To reset a local user's password, do the following:

. On the [.ui]*Security* screen, select [.ui]*Users & Groups*, and then select [.ui]*Users*.
+
image::manage-security/userSecurityRowClicked.png[,800,align=middle]

. Select the user account from the list for which you want to reset the password and select btn:[Reset Password] associated with that user.
The [.ui]*Reset Password* dialog is displayed.
+
image::manage-security/resetPassword.png[,260,align=left]

. In the [.ui]*Reset Password* dialog, enter the new password for the user in the [.ui]*New Password* field and re-enter to confirm the password in the [.ui]*Confirm Password* field.

. Select btn:[Save] to save the password changes.

The local user's password is reset.

[#force-password-update-ui]
=== Force Password Update

You can force a password update for any local user associated with your cluster.

You can either set a temporary password when creating a new user or force an existing user to change their password.

[#create-temporary-password]
==== Create a Temporary Password for a New User



===== Create a Temporary Password for a New User from the UI

To create a temporary password for a new user from the UI, do the following:

. On the [.ui]*Security* screen, select [.ui]*Users & Groups*, and then select [.ui]*Users*.

. Select btn:[Add User] to create a new user.

. In the [.ui]*Add New User* dialog, enter the following user's details:
** Enter the [.ui]*Username*.
** Enter a temporary password in the [.ui]*Password* field and re-enter it in the [.ui]*Verify Password* field.

. Select the necessary user roles and groups.
For more information, see xref:manage:manage-security/manage-users-and-roles.adoc#add-a-locally-authenticated-user[].

. Select btn:[Add User] to create the user with the temporary password.

The user can then log in with the temporary password and is prompted to change it upon first login.

===== Create a Temporary Password for a New User using the CLI






===== Create a Temporary Password for a New User using the REST API






[#force-password-update-existing-users]
==== Force Existing Users to Change Passwords





===== Force Existing Users to Change Passwords from the UI

To force existing users to change their passwords, do the following:

. On the [.ui]*Security* screen, select [.ui]*Users & Groups*, and then select [.ui]*Users*.

. Select the local user account from the list for which you want to force a password change and select btn:[Edit].

. In the [.ui]*Edit User* dialog, in the Force Password Update section, enable [.ui]*User must change password at next logon*.

. Select btn:[Save Changes].

The system prompts the user to change their password during their next login.

===== Force Existing Users to Change Passwords using the CLI






===== Force Existing Users to Change Passwords using the REST API







[#local-user-password-controls]
== Local User Password Controls

Local domain users can manage their own passwords, including changing and resetting them as needed.
In this scenario, they do not have to rely on administrators.

However, administrators can enforce password policies and restrictions on local user accounts to make sure of the security compliance.

=== Change Password On-demand

Local users can change their passwords whenever needed using the UI, CLI, or REST API.

==== Change Password On-demand from the UI

As a local user, to change the password whenever needed, do the following:

. Log in to the Couchbase Web Console.

. Select your username on the top-right corner and select [.ui]*Change Password*.

. In the [.ui]*Change Password* dialog, enter the current password in the [.ui]*Current Password* field, and then enter the new password in the [.ui]*New Password* field and re-enter it in the [.ui]*Confirm Password* field.

. Select btn:[Save] to save the password changes.

==== Change Password On-demand using the CLI

Local users can change their passwords using the Couchbase CLI command, xref:cli:cbcli/couchbase-cli-user-change-password.adoc[user-change-password].

The command is as follows:

----
/opt/couchbase/bin/couchbase-cli user-change-password --cluster http://10.144.210.101 \
--username Administrator \
--password password \
--new-password <new-password>
----

==== Change Password On-demand using the REST API

Local users can change their passwords using the Couchbase REST API method and URI `POST /controller/changePassword` as follows:

----
curl -X POST http://<ip-address-or-domain-name>:8091/controller/changePassword
  -u <username>:<password>
  -d <new-password>
----

An example command to change the password using the REST API is as follows:

----
curl -X POST http://localhost:8091/controller/changePassword \
-u localuser:password \
-d password=localpassword
----

Successful call returns `200 OK` and the local user password is changed.

=== Change Password Prompted by Administrator

To change your password when prompted by an administrator, do the following:

. Log in to the Couchbase Web Console.

. When you enter your current password, you're directed to the [.ui]*Provide New Password* dialog.

. Enter the new password in the [.ui]*New Password* field and re-enter it in the [.ui]*Confirm Password* field.

. Select btn:[Change Password] to save the new password.

For more information, see xref:manage:manage-security/manage-passwords.adoc#password-changes-for-local-users[] and xref:manage:manage-security/manage-passwords.adoc#force-password-update-existing-users[].