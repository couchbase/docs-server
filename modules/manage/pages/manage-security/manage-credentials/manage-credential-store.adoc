= Manage the Credential Store
:description: The credential store securely holds credentials that Couchbase Server services and users need to access external resources such as REST APIs and cloud storage.
:page-toclevels: 2
:page-edition: Enterprise Edition


[abstract]
{description}
For an overview of the credential store, see xref:learn:security/credential-store.adoc[].

== Configuration Overview

To use credentials to the credential store:

. Make sure your cluster meets the <<prerequisites,prerequisites>> for using the credential store.
. Add at least 1 credential to the credential store.
. Grant access to the credentials by:
** Granting users or groups access to a credential.
** Directly granting a service access to a credential.

[#prerequisites]
== Prerequisites

Before you can use the credential store, your cluster must meet all of the following requirements:

* All nodes in the cluster must be running Couchbase Server 8.5 or later.
The credential store does not work in mixed mode when any node in the cluster is running a version prior to 8.5.0.

* To create or alter credentials, your user account must have the xref:learn:security/roles.adoc#full-admin[Full Admin], xref:learn:security/roles.adoc#credential-admin[Credential Admin], or xref:learn:security/roles.adoc#security-admin[Security Admin] roles.

* Your cluster must have node-to-node encryption enabled.
This setting is not on by default.
This requirement protects the credentials as they're passed between nodes.
To learn how to enable node-to-node encryption, see xref:manage:manage-nodes/apply-node-to-node-encryption.adoc[].

* Your cluster must have encryption at rest enabled for configuration files. 
This encryption is on by default. 
This requirement prevents unauthorized parties from reading the credential store's secure data directly from disk. 
Once you have created a credential in the credential store, Couchbase Server prevents you from turning off encryption at rest for configuration files. 
For more information about encryption-at-rest, see xref:learn:security/native-encryption-at-rest-overview.adoc[].

NOTE: You can override the last 2 requirements. 
Only do so in test or development environments. 
Always use node-to-node encryption and encryption at rest for configuration files to secure credentials in production systems.
To learn how to override these requirements, see <<override-credential-store-requirements>>.

== Manage Credentials Using the Couchbase Server Web Console

You can view, add, and edit credentials using the Couchbase Server Web Console's *Credentials* page.
Open this page by clicking menu:Security[Credentials].

image::manage-security/credential-store/credentials-page.png[align=left]

=== Add a Credential Using the Couchbase Server Web Console

To add a new credential to the credential store:

. Open the *Credentials* by clicking menu:Security[Credentials].
. Click btn:[Add Credential].

+ 
image::manage-security/credential-store/add-credential-dialog.png[align=left]

. In the *Add Credential* dialog, enter an ID and optionally a description for the credential.
The ID is how users identify the credential when using it with Couchbase Server services. 
It can be up to 128 characters long and can only contain printable ASCII characters other than space and tab.

+
NOTE: While you can include symbols such as `*`, `#`, `?`, and `%` in an ID, the ID is used as part of the URI when making REST-API calls.
It's cleaner to limit symbols in IDs to URI-safe characters such as `/`, `_`, and `-`.

+
While not required, you may want to use a path-like naming convention to organize your credentials in a hierarchy.
For example, you can use IDs such as `query/rest-apis/stocks` and `backup/aws/s3`. 
This naming convention makes granting access to multiple credentials using wildcards easier.
//FIXME: add link to granting topic
For example, suppose you named several credentials following the previous examples.
Then you could grant a user access to all REST-API related credentials by using `query/rest-apis/*` or all backup-related credentials using `backup/*`.

. Under the *Enter Credentials* section, select the *Type* of credential you want to create.
Most of the entries in this field are based on external host provider (AWS or Couchbase, for example).
In some cases, there are several types for a single provider. 
For example, Azure has several subtypes.

+
The following table contains links to more information about ech authentication type:

+
[#credential-type-references]
.Credential Types: For More Information
[cols="1,2"]
|===
| Type | For More Information

| AWS
| https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_access-keys.html[Managing access keys for IAM users^]

| Azure: Shared Key
| https://learn.microsoft.com/en-us/rest/api/storageservices/authorize-with-shared-key[Authorize with Shared Key^]

| Azure: Active Directory
| https://learn.microsoft.com/en-us/azure/storage/blobs/authorize-access-azure-active-directory[Authorize access to blobs using Microsoft Entra ID^]

| Azure: SAS
| https://learn.microsoft.com/en-us/azure/storage/common/storage-sas-overview[Grant limited access to data with shared access signatures (SAS)^]

| Azure: Managed Identity
| https://learn.microsoft.com/en-us/entra/identity/managed-identities-azure-resources/overview[Managed identities for Azure resources^]

| GCP
a| 
* For JSON Credentials: https://docs.cloud.google.com/iam/docs/keys-create-delete[Create and delete service account keys^]
* For HMAC (Access Key ID): https://docs.cloud.google.com/storage/docs/authentication/hmackeys[HMAC keys^]

| Couchbase
| Currently unimplemented by any Couchbase feature.

| HTTP
| https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Authentication[HTTP authentication^] (MDN)

|===

. The other fields under the *Enter Credentials* section change depending on the type you chose. 
For assistance in filling in these fields, see the links in the previous table.

. In the *Guardrails* section, configure any guardrails you want to set for the credentials:
.. Optionally choose to the credential expire at a specific date and time.
For more information about credential expiration, see xref:learn:security/credential-store.adoc#expiration[Credential Expiration].

.. Choose which services can use the credential on behalf of the user. 
For more information, see xref:learn:security/credential-store.adoc##allowed-services[Which service can use the credential on behalf of a user].

+
NOTE: If you do not select at least 1 service under *Allowed Services*, users cannot the credential even if you grant them access.
Users do not directly use credentials. 
Instead, they rely on a service to use it on their behalf.
If you do not authorize a service to use the credential on a user's behalf, users cannot use it.

.. If the credential will authenticate with URLs, decide which sites are valid.
Select *All Access (allow all URLs)* to allow users to use the credential to authenticate with any site they choose.
Otherwise, enter a list of URLs the user can authenticate with. 
Optionally add a list of endpoints within the allowed URLs that the user cannot authenticate with.
For more information, see xref:learn:security/credential-store.adoc#url-restrictions[URL and endpoint restrictions].


. Click btn:[Add Credential] to save the credential.

=== Viewing and Editing Credentials Using the Couchbase Server Web Console

To view a credential's details or edit it, click *Edit* in the credential's entry 
on the Credential's page. 

You cannot change the ID or type of a credential. 

IMPORTANT: When you edit a credential, the fields containing secret information (such as AWS's *Secret Access Key* field and HTTP's *Token* field) are blank. 
Once you have set a secret value, Couchbase Server never displays it again.
When editing a credential, you must re-enter the secret values again even if you want to keep the existing values.
If a secret field is required, you must enter a value in it before you an save the credential.

=== Delete Credentials Using the Web Console

To delete a credential using the Couchbase Server Web Console:

. Open the *Credentials* page by clicking menu:Security[Credentials].
. Click btn:[Delete] in entry for the credential you want to delete.

include::manage:partial$credential-delete-caution.adoc[]

== Manage Credentials Using the REST API

Use the `/settings/credentials/{credential_id}` endpoint to add, retrieve, edit an drop credentials in the credential store.

[#rest-api-add]
=== Add a Credential Using the REST API

You must always supply the credential ID as a path parameter in the URI.
A `curl` command that calls the endpoint to add a credential looks like this: 

[source,bash]
----
curl -u $CB_USER:$CB_PASS -X POST \
     'http://{HOST}:{PORT}/settings/credentials/<credential-id>' \
     -H 'Content-Type: application/json' \
     -d '{
           "type": "<credential-type>",
           "description": "<text>",
           "expiresAt": <epoch-ms>,
           "fields": {<type-specific-fields>},
           "guardrails": {<guardrail-definition>}
         }'
----

* `type` is a required string the sets what authentication provider the credential is for. See the next section for details.
* `description` is an optional text description.
* `expiresAt` is an optional integer that sets when the credential expires.
Its value is in milliseconds since the UNIX epoch (midnight January 1st 1970).
* `fields` is an object that contains the settings for the credential.
Its contents depend on the value you set in `type`.
See <<type-fields>> for more information.
* `guardrails` is an object that sets limitations on the credential's use. 

The following sections describe the `fields` and `guardrails` objects.

[#type-fields]
==== Credential Type and Fields

The required and optional fields for the REST API call depends on the `type` value.
The following table lists the supported types, the literal value for the type field, the required and optional fields in the `fields` object.

The following expandable list gives the value for the `type` field and the associated required and optional fields in the `fields` object.

.AWS
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* | https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_access-keys.html[Manage access keys for IAM users^]
| *Type Value* | `aws`
a| *Required Fields* a|
* `accessKeyId` - AWS access key ID
* `secretAccessKey` - AWS secret access key
* `region` - AWS region (e.g. `us-east-1`)
a| *Optional Fields* a|
* `endpoint` - S3-compatible endpoint override
* `sessionToken` - Temporary session token (STS)
|===
=====

.AWS: Instance Metadata (IMDS)
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* | https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/iam-roles-for-amazon-ec2.html[IAM roles for Amazon EC2^]
| *Type Value* | `awsInstanceMetadata`
a| *Required Fields* a|
* `region` - AWS region (e.g. `us-east-1`)
a| *Optional Fields* a|
* `endpoint` - S3-compatible endpoint override
|===
=====

.Azure: Shared Key
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* | https://learn.microsoft.com/en-us/rest/api/storageservices/authorize-with-shared-key[Authorize with Shared Key^]
| *Type Value* | `azureShared`
a| *Required Fields* a|
* `accountName` - Azure storage account name
* `accountKey` - Azure storage account key
a| *Optional Fields* a|
* `endpoint` - Custom endpoint override
|===
=====

.Azure: Active Directory
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* | https://learn.microsoft.com/en-us/azure/storage/blobs/authorize-access-azure-active-directory[Authorize access to blobs using Microsoft Entra ID^]
| *Type Value* | `azureAd`
a| *Required Fields* a|
* `clientId` - Azure AD application client ID
* `tenantId` - Azure AD tenant ID
* `clientSecret`, or `certificate` - Client secret, or client certificate (PEM); exactly one is required
a| *Optional Fields* a|
* `certPassword` - Password for an encrypted certificate
* `endpoint` - Custom endpoint override
|===
=====

.Azure: SAS
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* | https://learn.microsoft.com/en-us/azure/storage/common/storage-sas-overview[Grant limited access to data with shared access signatures (SAS)^]
| *Type Value* | `azureSas`
a| *Required Fields* a|
* `accountName` - Azure storage account name
* `sharedAccessSignature` - SAS token
a| *Optional Fields* a|
* `endpoint` - Custom endpoint override
|===
=====

.Azure: Managed Identity
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* | https://learn.microsoft.com/en-us/entra/identity/managed-identities-azure-resources/overview[Managed identities for Azure resources^]
| *Type Value* | `azureManaged`
a| *Required Fields* a|
* none
a| *Optional Fields* a|
* `managedIdentityId` - Managed identity client ID (optional for system-assigned identities)
* `endpoint` - Custom endpoint override
|===
=====

.GCP
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* a|
* https://docs.cloud.google.com/iam/docs/keys-create-delete[Create and delete service account keys^]
* https://docs.cloud.google.com/storage/docs/authentication/hmackeys[HMAC keys^]
| *Type Value* | `gcp`
a| *Required Fields* a|
* `jsonCredentials` - GCP service-account JSON key file, or
* `accessKeyId` and `secretAccessKey` - HMAC access key and secret key
a| *Optional Fields* a|
* `region` - GCS region
* `endpoint` - Custom endpoint override
|===
=====

.GCP: Application Default Credentials (ADC)
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* | https://docs.cloud.google.com/docs/authentication/application-default-credentials[How Application Default Credentials works^]
| *Type Value* | `gcpAdc`
a| *Required Fields* a|
* none
a| *Optional Fields* a|
* none
|===
=====

.HTTP
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* | https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Authentication[HTTP authentication^]
| *Type Value* | `http`
a| *Required Fields* a|
* `authScheme` - Authentication scheme: `basic`, `bearer`, or `mtls`
* Scheme-dependent: +
  Basic: `username` - Username; `password` - Password +
  Bearer: `token` - Bearer token +
  mTLS: `certificate` - Client certificate (PEM); `privateKey` - Client private key (PEM)
a| *Optional Fields* a|
* `headerName` - Custom header name (Bearer only; defaults to `Authorization`)
* `rootCertificate` - CA / root certificate (PEM)
* `passphrase` - Passphrase for an encrypted private key (mTLS)
* `skipVerify` - Skip TLS verification (development only)
|===
=====

.Couchbase
[%collapsible]
=====
[cols="1,3"]
|===
| *Documentation Link* |
| *Type Value* | `couchbase`
a| *Required Fields* a|
* `encryptionType` - `none`, `half`, or `full`
a| *Optional Fields* a|
* `username` - Remote cluster username
* `password` - Remote cluster password
* `certificate` - Client certificate (PEM)
* `privateKey` - Client private key (PEM)
* `passphrase` - Passphrase for an encrypted private key
* `rootCertificate` - Remote cluster CA certificate (PEM)
|===
=====

For example, the `fields` object for an AWS credential (including the optuional values) looks like this:

[source,json]
----
"fields": {
  "accessKeyId": "AKIAIOSFODNN7EXAMPLE",
  "secretAccessKey": "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY",
  "region": "us-east-1",
  "endpoint": "https://s3.us-east-1.example-cloud.com",
  "sessionToken": "FQoGZXIvYXdzEXAMPLESESSIONTOKEN1234567890abcdefEXAMPLE=="
}
----

The `fields` object for an HTTP bearer token looks like this:

[source,json]
----
"fields": {
  "authScheme": "bearer",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example.token",
  "headerName": "X-Api-Key",
  "rootCertificate": "-----BEGIN CERTIFICATE-----\nMIIDXTCCAkWgAwIBAgIJAExampleCertData...\n-----END CERTIFICATE-----",
  "skipVerify": false
}
----

==== Guardrails Object

The `guardrails object` limits how a user can use a credential.
The object is optional. 
If you do not supply it, the credential cannot be used by users for any purpose.
It can only be used by services you directly grant the right to use the credential.  
For more information, see xref:learn:security/credential-store.adoc#guardrails[Guardrails]


[source,json]
----
"guardrails": {
  "allowedServices": ["<service>", "..."],   
  "urlWhitelist": {
    "allAccess": <true|false>, 
    "allowedUrls": ["<url-prefix>", "..."],  
    "disallowedUrls": ["<url-prefix>", "..."] 
  }
}
----

All of the fields in this object are optional. 

* `allowedServices` is a list of strings that sets the Couchbase Server services that can use the credential on behalf of the user. Valid entries in this list are: `n1ql` (Query Service), `backup`, `index`, `xdcr`, `fts`, `eventing`, `cbas` (Analytics Service).
* `allAccess` a Boolean that controls whether the credential can be used for all URLs. 
Defaults to `false`.
* `allowedUrls` lists URL prefixes the credential can authenticate with. 
* `disallowedUrls` is a list of endpoints in the list of allowed URLs that teh credential is not allowed to authenticate with.

For details on these settings, see XXX.

For example, the following `guardrails` object lets a user call the DDL `CURL()` function (provided by the Query Service) to authenticate with any REST API endpoint below  `https://example.com/rest-apis/` except the `/rest-apis/forbidden-endpoint` or anything below it.

[source,json]
----
"guardrails": {
  "allowedServices": ["n1ql"],
  "urlWhitelist": {
    "allAccess": false,
    "allowedUrls": ["https://example.com/rest-apis/"],
    "disallowedUrls": ["https://example.com/rest-apis/forbidden-endpoint"]
  }
}
----

==== Creating Credential Using the REST API Examples

[#aws-example]
The following example creates an AWS credential for use with an Apache Iceberg external collection.

// FIXME: link to external collection docs when done.

[source,bash]
----
curl -u $USERNAME:$PASSWORD -X POST \
     'http://{HOST}:{PORT}/settings/credentials/iceberg/aws/sales-data' \
     -H 'Content-Type: application/json' \
     -d '{
           "type": "aws",
           "description": "S3 access for the sales-data Iceberg table",
           "fields": {
             "accessKeyId": "AKIAIOSFODNN7EXAMPLE",
             "secretAccessKey": "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY",
             "region": "us-east-1"
           },
           "guardrails": {
             "allowedServices": ["n1ql"]
           }
         }'
----

Because this credential is for AWS, the only guardrail it needs is to allow the Query Service to use it on behalf of a user.


[#http-example]
The next example demonstrates creating an HTTP bearer token for the https://massive.com[Massive] REST API for accessing stock information.

[source,bash]
----
curl -u $USERNAME:$PASSWORD -X POST \
     'http://{HOST}:{PORT}/settings/credentials/stocks/massive/api-key' \
     -H 'Content-Type: application/json' \
     -d '{
           "type": "http",
           "description": "Massive stock market API",
           "expiresAt": 1820966400000,
           "fields": {
             "authScheme": "bearer",
             "token": "yOuRMaSsIvEaPiKeYgOeShErE1234567890"
           },
           "guardrails": {
             "allowedServices": ["n1ql"],
             "urlWhitelist": {
               "allAccess": false,
               "allowedUrls": ["https://api.massive.com/"]
             }
           }
         }' | jq
----

If the command succeeds, Couchbase Sever returns a JSON object containing the new credential:

[source,json]
----
{
  "fields": {
    "authScheme": "bearer",
    "token": "********"
  },
  "id": "stocks/massive/api-key",
  "meta": {
    "createdAt": 1789498724446,
    "createdBy": {
      "domain": "admin",
      "user": "Administrator"
    },
    "description": "Massive stock market API",
    "expiresAt": 1820966400000,
    "guardrails": {
      "allowedServices": [
        "n1ql"
      ],
      "urlWhitelist": {
        "allAccess": false,
        "allowedUrls": [
          "https://api.massive.com/"
        ]
      }
    },
    "payloadVersion": "g2gCbQAAACBjYmVjZDEyYTNiYzVjZTg0NzdhYjhkMzI1YTk3MmI4OWF/",
    "secretSetAt": 1789498724446,
    "secretSetBy": {
      "domain": "admin",
      "user": "Administrator"
    }
  },
  "schemaVersion": 1,
  "type": "http"
}
----

Notice that Couchbase Server always redacts the sensitive fields (`token` in the previous example).
Once you have created the credential, you cannot retrieve these values from Couchbase Server.

=== Viewing Credentials Using the REST API

Make a `GET` request to the  `/settings/credentials` endpoint to list all of the credentials defined in the credential store.
To get the details of a specific credential, make a `GET` request to `/settings/credentials/{credential_id}`

Your user account must have the xref:learn:security/roles.adoc#full-admin[Full Admin], xref:learn:security/roles.adoc#credential-admin[Credential Admin], xref:learn:security/roles.adoc#security-admin[Security Admin], or xref:learn:security/roles.adoc#ro-security-admin[Read-Only Security Admin] roles.

The following example demonstrates getting all credentials defined in the credential store:

[source,json]
----
curl -u Administrator:password -X GET \
     'http://node1.example.com:8091/settings/credentials/'  | jq
----

The following shows an example of running the previous command:

[source,json]
----
{
  "credentials": [
    {
      "fields": {
        "accessKeyId": "AKIAFAKEEXAMPLE02Q",
        "region": "us-east-1",
        "secretAccessKey": "********"
      },
      "id": "analytics/aws/glue-catalog",
      "meta": {
        "createdAt": 1789133131200,
        "createdBy": {
          "domain": "admin",
          "user": "Administrator"
        },
        "description": "AWS credential for Iceberg external catalog via AWS Glue (demo/fake)",
        "expiresAt": 1820669258000,
        "guardrails": {
          "allowedServices": [
            "n1ql"
          ]
        },
        "payloadVersion": "g2gCbQAAACBjYmVjZDEyYTNiYzVjZTg0NzdhYjhkMzI1YTk3MmI4OWFv",
        "secretSetAt": 1789133274466,
        "secretSetBy": {
          "domain": "admin",
          "user": "Administrator"
        },
        "updatedAt": 1789133274466,
        "updatedBy": {
          "domain": "admin",
          "user": "Administrator"
        }
      },
      "schemaVersion": 1,
      "type": "aws"
    },
    {
      "fields": {
        "accountKey": "********",
        "accountName": "fakebackupstorage01"
      },
      "id": "backup/azure/blob-storage",
      "meta": {
        "createdAt": 1789133131237,
        "createdBy": {
          "domain": "admin",
          "user": "Administrator"
        },
        "description": "Azure Blob Storage credential for backup archive (demo/fake)",
        "expiresAt": 1820669258000,
        "guardrails": {
          "allowedServices": [
            "backup"
          ]
        },
        "payloadVersion": "g2gCbQAAACBjYmVjZDEyYTNiYzVjZTg0NzdhYjhkMzI1YTk3MmI4OWFw",
        "secretSetAt": 1789133274506,
        "secretSetBy": {
          "domain": "admin",
          "user": "Administrator"
        },
        "updatedAt": 1789133274506,
        "updatedBy": {
          "domain": "admin",
          "user": "Administrator"
        }
      },
      "schemaVersion": 1,
      "type": "azureShared"
    },
    {
      "fields": {
        "authScheme": "bearer",
        "token": "********"
      },
      "id": "stocks/massive/api-key",
      "meta": {
        "createdAt": 1789498724446,
        "createdBy": {
          "domain": "admin",
          "user": "Administrator"
        },
        "description": "Massive (formerly Polygon.io) stock market API",
        "expiresAt": 1820966400000,
        "guardrails": {
          "allowedServices": [
            "n1ql"
          ],
          "urlWhitelist": {
            "allAccess": false,
            "allowedUrls": [
              "https://api.massive.com/"
            ]
          }
        },
        "payloadVersion": "g2gCbQAAACBjYmVjZDEyYTNiYzVjZTg0NzdhYjhkMzI1YTk3MmI4OWF/",
        "secretSetAt": 1789498724446,
        "secretSetBy": {
          "domain": "admin",
          "user": "Administrator"
        }
      },
      "schemaVersion": 1,
      "type": "http"
    }
  ],
  "warnings": [
    "Stored credentials risk being sent unencrypted unless node-to-node encryption is enabled on every node in the cluster"
  ]
}
----

In addition to the 3 credentials, the request also returns a warning about an override to the node-to-node encryption requirement.

=== Editing Credentials Using the REST API

You have 2 ways to alter a credential using the REST API: the `PATCH` method and the `PUT` method.
The `PATCH` method lets you modify some information in the credential without having to rewrite all of the fields (including the fields containing secrets).
The `PUT` method completely replaces the credential with the new values you supply.
Any values you do not supply are cleared.

==== Using the PATCH Method

If you just want to edit a credential's metadata (its description, expiration, and guardrails) you can make a `PATCH` method to `/settings/credentials/{credential_id}`. 
This method call leaves values that you do not alter 

NOTE: While `PATCH` allows for in-place modification, if you alter any field in the `guardrails` object, it replaces the entire object. 
Therefore, if want to change some of the guardrail values and keep the others as-is, you must supply all of them in call.

For example, the following example change the HTTP credential created in a <<http-example,previous example>>.
It reduces the expiration by 6 months from September 15, 2027 to March 15th, 2027.  
It also adds the Analytics Service (`cbas`) to the list of services that can use the credential on behalf of the user.

[source,bash]
----
curl -u $USERNAME:$PASSWORD -X PATCH \
     'http://node1.example.com:8091/settings/credentials/stocks/massive/api-key' \
     -H 'Content-Type: application/json' \
     -d '{
           "expiresAt": 1805068800000,
           "guardrails": {
             "allowedServices": ["n1ql", "cbas"],
             "urlWhitelist": {
               "allAccess": false,
               "allowedUrls": ["https://api.massive.com/"]
             }
           }
         }' | jq
----

The result of running the previous example looks like this:

[source,json]
----
{
  "fields": {
    "authScheme": "bearer",
    "token": "********"
  },
  "id": "stocks/massive/api-key",
  "meta": {
    "createdAt": 1789498724446,
    "createdBy": {
      "domain": "admin",
      "user": "Administrator"
    },
    "description": "Massive (formerly Polygon.io) stock market API",
    "expiresAt": 1805068800000,
    "guardrails": {
      "allowedServices": [
        "n1ql",
        "cbas"
      ],
      "urlWhitelist": {
        "allAccess": false,
        "allowedUrls": [
          "https://api.massive.com/"
        ]
      }
    },
    "payloadVersion": "g2gCbQAAACBjYmVjZDEyYTNiYzVjZTg0NzdhYjhkMzI1YTk3MmI4OWGD",
    "secretSetAt": 1789498724446,
    "secretSetBy": {
      "domain": "admin",
      "user": "Administrator"
    },
    "updatedAt": 1789504728769,
    "updatedBy": {
      "domain": "admin",
      "user": "Administrator"
    }
  },
  "schemaVersion": 1,
  "type": "http"
}
----

==== Using the PUT Method

Using the `PUT` method is almost the same as using the `POST` method.
The exceptions are:

* The credential ID must already exist.
If you attempt to `PUT` to a credential URI that does not exist (for example `/settings/credentials/non-existent-credential`), you'll get an error.
* The `type` you supply in the call must match the `type` of the existing credential.
You cannot change the type of a credential once you have created it.

Otherwise, you can change any value in the `PUT` call.
If you do not supply a replacement value for a populated field in the credential, the field's value is wiped clean.
This includes the secret values.

The following example alters the AWS credential created in <<aws-example,an earlier example>> by changing its region from `us-east-1` to `eu-west-1`.

[source,bash]
----
curl -u $USERNAME:$PASSWORD -X PUT \
     'http://{HOST}:{PORT}/settings/credentials/iceberg/aws/sales-data' \
     -H 'Content-Type: application/json' \
     -d '{
           "type": "aws",
           "description": "S3 access for the sales-data Iceberg table",
           "fields": {
             "accessKeyId": "AKIAIOSFODNN7EXAMPLE",
             "secretAccessKey": "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY",
             "region": "eu-west-1"
           },
           "guardrails": {
             "allowedServices": ["n1ql"]
           }
         }'
----

=== Deleting Credentials Using the REST API

To delete a credential, make a `DELETE` method call to the `/settings/credentials/{credential-id}` endpoint:

[source,bash]
----
curl -u $USERNAME:$PASSWORD -X DELETE \
     'http://{HOST}:{PORT}/settings/credentials/{credential-id}'
----

include::manage:partial$credential-delete-caution.adoc[]

This example drops the HTTP credential created in a <<http-example,previous example>>:

[source,bash]
----
curl -u $USERNAME:$PASSWORD -X DELETE \
     'http://node1.example.com:8091/settings/credentials/stocks/massive/api-key'
----

A successful call returns the status code `200` and no other object. 

== Managing Credentials Using DDL

You can use {sqlpp} statements to create, alter, and drop credentials.

=== Create a Credential Using DDL

Use the `CREATE CREDENTIALSTORE` statement to add a credential:

[source,sqlpp]
----
CREATE CREDENTIALSTORE `<id>` WITH {
    "type": "<credential-type>",
    "description": "<text>",
    "expiresAt": <epoch-ms>,
    "fields": {<type-specific-fields>},
    "guardrails": {<guardrail-definition>}
};
----

The content of the `WITH` clause is a JSON object with the same as the REST API's payload. 
For the structure of the values in this clause, see <<rest-api-add>>.

The following example creates an AWS credemtial for use with an Apache Iceberg external collection.

// FIXME: add link to external collections doc when written.

[source,sqlpp]
----
CREATE CREDENTIALSTORE `iceberg/aws/sales-data` WITH {
  "type": "aws",
  "description": "S3 access for the sales-data Iceberg table",
  "fields": {
    "accessKeyId": "AKIAIOSFODNN7EXAMPLE",
    "secretAccessKey": "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY",
    "region": "us-east-1"
  },
  "guardrails": {
    "allowedServices": ["n1ql"]
  }
};
----

[#ddl-create-example]
This example creates an HTTP bearer token for a REST API.

[source,n1ql]
----
CREATE CREDENTIALSTORE `stocks/massive/api-key` WITH {
  "type": "http",
  "description": "Massive stock market API",
  "expiresAt": 1820966400000,
  "fields": {
    "authScheme": "bearer",
    "token": "yOuRMaSsIvEaPiKeYgOeShErE1234567890"
  },
  "guardrails": {
    "allowedServices": ["n1ql"],
    "urlWhitelist": {
      "allAccess": false,
      "allowedUrls": ["https://api.massive.com/"]
    }
  }
};
----

=== Altering Credentials Using DDL

Altering an existing credential using DDL is similar to creating one:

[source,n1ql]
----
ALTER CREDENTIALSTORE `<id>` WITH {
  "type": "<credential type>",
  "fields": {
    <type-specific fields>
  },
  "guardrails": {
    <guardrail fields>
  }
};
----

As with `CREATE`, the content of the `WITH` clause is the same as the REST API call's payload. 
For the structure of the values in this clause, see <<rest-api-add>>.

The `ALTER` operation is a full replacement.
If you do not supply a value for a field, any existing value is dropped.

The following example alters the HTTP credential created in an <<ddl-create-example,earlier example>> by adding the Analytics Service (`cbas`) to the list of allowed services.

[source,sqlpp]
----
ALTER CREDENTIALSTORE `stocks/massive/api-key` WITH {
  "type": "http",
  "description": "Massive stock market API",
  "expiresAt": 1820966400000,
  "fields": {
    "authScheme": "bearer",
    "token": "yOuRMaSsIvEaPiKeYgOeShErE1234567890"
  },
  "guardrails": {
    "allowedServices": ["n1ql", "cbas"],
    "urlWhitelist": {
      "allAccess": false,
      "allowedUrls": ["https://api.massive.com/"]
    }
  }
};
----

=== Delete a Credential Using DDL

Use `DROP CREDENTIALSTORE` to delete a credential:

[source,sqlpp]
----
DROP CREDENTIALSTORE `<id>`;
----

For example, to drop the HTTP credential from the credential store: 

[source,sqlpp]
----
DROP CREDENTIALSTORE `stocks/massive/api-key`;
----

[#next-steps]
== Next Steps

* To grant users, groups, and services access to the credentials you've created, see xref:manage-security/manage-credentials/manage-user-access-credentials.adoc[].

