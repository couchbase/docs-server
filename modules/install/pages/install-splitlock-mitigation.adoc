= Split Lock Mitigation

:description: Learn how to disable Linux split lock mitigation on affected systems
:page-topic-type: procedure
:keywords: troubleshooting, server

[abstract]
{description}

Atomic instructions can sometimes involve memory changes over multiple cache lines.
To ensure consistency across caches, some CPUs may lock the bus, preventing other processes from accessing the memory.
This is known as a split lock.

On some Linux kernels that support split lock detection, the kernel's split lock mitigation can cause a severe performance drop when a process triggers a misaligned atomic memory access.
This may appear as high latency and reduced throughput under load.

Disabling split lock mitigation changes kernel behavior intended to reduce the impact of split locks.
Apply this setting only on systems where you have confirmed it's contributing to performance degradation.

If you observe this behavior on Couchbase Server hosts, disable split lock mitigation.

== How to Apply

Choose 1 of the following methods.

. Kernel boot parameter (recommended for permanence)
+
Add the following kernel command-line parameter and reboot the host:
+
[source,bash]
----
# Example kernel cmdline parameter
split_lock_detect=off
----

. Runtime setting (if supported by your kernel)
+
Disable mitigation via `/proc`:
+
[source,bash]
----
echo 0 > /proc/sys/kernel/split_lock_mitigate
----
+
NOTE: This change may not persist across reboot unless you also configure it to be applied at boot.
